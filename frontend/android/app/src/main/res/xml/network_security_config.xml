<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow all HTTPS traffic (default) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Explicitly allow our production backend over HTTPS -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">nitisetu-backend-4p2o.onrender.com</domain>
        <domain includeSubdomains="true">nitisetu-frontend.onrender.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Allow localhost for Capacitor bridge (dev only, not included in release) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
